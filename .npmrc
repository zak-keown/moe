# GitLab instance-level npm registry. The @bubstack scope must match the
# top-level GitLab group name for this endpoint to resolve.
@bubstack:registry=https://gitlab.tcdevops.com/api/v4/packages/npm/
