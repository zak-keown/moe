# Transaction fixtures

The transaction suite constructs each fault state explicitly so the expected
generation is visible at the assertion site. This directory reserves the
fixture surface for restart cases that need on-disk trees.
