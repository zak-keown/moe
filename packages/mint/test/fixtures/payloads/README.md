# Payload fixtures

The payload suite builds isolated source trees from these fixture semantics so
it can create POSIX-only hostile entries (symbolic links and hard links) and
binary bytes without making the repository checkout itself unsafe.
