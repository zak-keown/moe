# @bubstack/moe-backstory

Recover a behavioral spec from a codebase that never had one.

Ships as the **`moe-backstory`** plugin, generated into `/plugins/moe-backstory` by `@bubstack/moe-mint`. Never hand-edit the generated manifest.

**Status:** scaffold. No code imported yet.

## Forked from

| Upstream repo | Pinned | License |
|---|---|---|
| `greenfield` | `6e6d4b4` | Apache-2.0 |

Snapshots are in `.references/` (gitignored). They are the spec — not upstream
`main`. See [PARITY.md](../../PARITY.md).

## Import notes

- 22 skills and 2 agents. The only content package that stays out of core.
- Apache-2.0 requires a statement of changes; the rebrand is a change.
