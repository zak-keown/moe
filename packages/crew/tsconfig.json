{
  "//": "Runtime DAG for this package: no workspace dependencies, so no references. The pi extension imports @earendil-works/pi-coding-agent for types only; the hook and CLI bundles resolve each other by file path at runtime, not as module edges.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "sourceMap": false,
    "declarationMap": false
  },
  "include": ["src/**/*"],
  "references": []
}
