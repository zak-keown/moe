{
  "//": "Runtime DAG for this package: no workspace dependencies, so no references. Everything it needs is a registry dependency; the MCP server, the CLI and the hook all reach each other as compiled siblings under dist/, not across packages.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "references": []
}
