---
context: default
phase: skill-backend-runtime-standard
task: 1
total_tasks: 7
status: in_progress
last_updated: 2026-09-03T22:51:02.878Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Isolated worktree only — multiple fresh subagents initially opened the primary checkout even when given the worktree path — `cd /Users/zakkeown/Code/tools/moe/.moe/worktrees/feature-skill-backend-runtime`, verify branch/HEAD/status, and never modify the primary checkout.
- [ ] CONSTRAINT: Preserve interrupted Task 1 WIP — the tree contains substantial uncommitted work with no final verification/report — inspect and continue it; do not reset, discard, or declare it complete.
- [ ] CONSTRAINT: Node 24 and bounded runners — the ambient Node is 22 and pnpm wrappers repeatedly stalled silently — use `mise exec node@24 --` with package-local binaries and bounded waits; isolate/terminate a command after repeated silence.
- [ ] CONSTRAINT: Generated plugins are deferred — hand-editing `/plugins/` violates repository law — touch generated plugins only in Plan 5 via `pnpm mint`.
- [ ] CONSTRAINT: Activation carry must close — Unicode-decimal `chunk_spec --max-tokens` remains a known Important parity gap — Plan 5 Task 1 must add the regression/fix before enabling either Mint gate.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Wrong checkout | Workers repeatedly searched the primary checkout and missed worktree-local SDD artifacts. | blocking | Begin every worker/reviewer prompt and command by changing to the exact worktree and checking `git rev-parse --show-toplevel`, HEAD, and status. |
| Silent runner hang | Several pnpm wrapper invocations produced no output for minutes and blocked workers. | blocking | Use Node 24 via Mise, package-local test binaries, bounded polling, and interrupt after the second silent attempt. |
| Assuming interrupted work is green | Task 1 has source/tests/docs edits but no report or trustworthy completion signal. | blocking | Reconstruct RED/GREEN state from the diff and rerun focused/full gates before commit or review. |
| Extra final fix wave | Plan 2 final re-review found one residual after its single permitted whole-plan wave. | advisory | Keep the adjudicated carry in Plan 5 Task 1; do not rewrite Plan 2 history or silently patch it out of sequence. |

<current_state>

Paused in plan set `skill-backend-runtime-standard`, Plan 3 (`core-process`), Task 1 of 7. Branch `feature-skill-backend-runtime`, isolated worktree `/Users/zakkeown/Code/tools/moe/.moe/worktrees/feature-skill-backend-runtime`. The WIP handoff commit sits directly on implementation base `0d949d1` (`docs: mark core data migration complete`). The interrupted worker left 23 dirty paths covering the expected CJS→ESM rename, render-graphs move, new Vitest suites, review-helper normalization, docs, package scripts, and metadata. No Task 1 report or implementation commit exists.

Context detection found no active `.planning/phases`, spike, sketch, or deliberation directory for this work. This is a plan-set execution outside Moe's phase layout, so the handoff lives at `.planning/.continue-here.md`.

</current_state>

<completed_work>

- Baseline repaired and independently reviewed. Fresh baseline passed `pnpm check` and `pnpm mint:check` before migrations.
- Plan 1 `runtime-validator` completed. Key commits: `4ad11a2` through `6221ce1`; manifest bookkeeping `92f33eb`. Two validator bypasses are carried into Plan 5 Task 1.
- Plan 2 `core-data` completed and manifest-marked done at `0d949d1`. Ten Python skill helpers were migrated to Node 24 ESM with 148 iterative tests and 387 Core tests green at final-wave commit `952a747`.
- Plan 2's whole-plan wave resolved unsafe JSON integers, symlink entry, Python newline/Unicode/sorting, argparse, and documentation findings. One Unicode-decimal token-count edge remains explicitly carried to Plan 5.
- Plan 3 preflight/ledger and Task 1 brief were written under `.moe/sdd/2026-09-03-skill-runtime-03-core-process/`.
- Plan 3 Task 1 is in progress only. Its interrupted delta exists but is unverified and uncommitted.

</completed_work>

<remaining_work>

- Plan 3 Task 1: inspect the current dirty delta; establish which tests were written and whether RED/GREEN occurred; finish `update_docs.mjs`, `render-graphs.mjs`, docs-report test migration, review-helper normalization, active references, modes, and all gates; write report; commit; run independent task review/fix loop.
- Plan 3 Tasks 2–7: duplicate-analysis pipeline; SDD utilities; Git/tmux/Crew launchers; brainstorm ESM; brainstorm lifecycle; metadata reconciliation.
- Plan 3 whole-plan review and single final fix wave/re-review.
- Plan 4 Glass ESM migration, including task reviews and whole-plan review.
- Plan 5 enforcement activation: close both validator bypass carries and Unicode-decimal chunk carry; wire Mint pre-staging and live-repository gates; update architecture/guidance; generate plugins; run complete qualification.

</remaining_work>

<decisions_made>

- Shipped skill backend boundary: dependency-free Node 24 ESM `.mjs` under each skill's `scripts/`, mode 0644, no shebang, only `node:` built-ins/relative `.mjs`, explicit plugin-rooted `node` invocation.
- Package `src/` remains TypeScript/compiled output; declared Git/tmux/Chrome capabilities remain valid external requirements.
- `/plugins/` is generated only by Mint in Plan 5; package sources are authoritative.
- Core's `scripts/validate_skill.py`, its six-test module, and empty `__init__.py` are not shipped skill backend and remain out of scope.
- SDD execution is sequential in this shared worktree: fresh implementer, independent task review/fix/re-review, then one whole-plan final review/fix/re-review.
- The Plan 2 Unicode-decimal argparse residual is carried—not forgotten—to Plan 5 Task 1, with gate activation blocked until fixed.

</decisions_made>

<blockers>

- No external/human blocker.
- Technical pause state: Task 1 is dirty and lacks a report/final tests.
- Environment: default Node 22 and intermittently hanging pnpm wrapper; use Mise Node 24 and package-local commands.

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — repository law and verification contract.
2. `ARCHITECTURE.md` — approved architecture and local prerequisites.
3. `docs/moe/specs/2026-09-03-skill-backend-runtime-standard-design.md` — runtime boundary.
4. `docs/moe/plans/skill-backend-runtime-standard-MANIFEST.md` — plan-set state and order.
5. `docs/moe/plans/2026-09-03-skill-runtime-03-core-process.md` — current plan.
6. `.moe/sdd/2026-09-03-skill-runtime-03-core-process/progress.md` — preflight and execution ledger.
7. `.moe/sdd/2026-09-03-skill-runtime-03-core-process/task-1-brief.md` — exact interrupted-task ownership and gates.
8. `docs/moe/plans/2026-09-03-skill-runtime-05-activation.md` — mandatory carried gaps before enforcement.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run or delegate from the primary checkout. Always verify the exact isolated worktree first.
- Do not discard the Task 1 dirty state or fold it into an unrelated commit.
- Do not trust silent pnpm wrappers as evidence; rerun bounded targets under Mise Node 24.
- Do not hand-edit generated plugins.
- Do not mark a task/plan complete before its required independent review and fresh verification.

## Infrastructure State

- Active branch/worktree: `feature-skill-backend-runtime` at the isolated path above.
- Primary checkout: contains unrelated user changes and remains untouched.
- Background agents/processes: active Task 1 agent was interrupted; no intentional server, watcher, or async external job is running.
- Runtime: ambient Node 22.23.2; required Mise Node 24.20.0/pnpm 11.23.0.
- Git state: current HEAD is the WIP handoff commit whose parent is `0d949d1`; the worktree remains dirty only with interrupted Plan 3 Task 1 files.

<context>

The migration is deliberately compatibility-first. Task-local reviews repeatedly found implicit Python semantics (path spelling, Unicode digits, code-point sorting, universal newlines, bool/int equality), so Plan 3 ports should characterize frozen behavior and include differential edge tests rather than transliterating syntax. For process helpers, preserve argv arrays and forbid shell evaluation. The immediate WIP appears structurally aligned with Task 1 but has not earned a completion claim.

</context>

<next_action>

Start with: `cd /Users/zakkeown/Code/tools/moe/.moe/worktrees/feature-skill-backend-runtime`; verify branch `feature-skill-backend-runtime`, verify `git rev-parse HEAD^` is `0d949d1...`, and confirm the recorded dirty paths. Read the Task 1 brief, inspect the full diff/new files, then rerun the focused Task 1 tests under Mise Node 24 to determine the true RED/GREEN state before making further edits.

</next_action>
