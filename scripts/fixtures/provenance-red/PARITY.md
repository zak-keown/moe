# Import ledger (fixture)

A deliberately minimal stand-in for the real PARITY.md, so the provenance check
can be shown failing. One upstream row, and nothing else the parser might latch
onto.

## Map

| Upstream repo | Pinned | License | → lands as |
|---|---|---|---|
| `gauntlet` | `91b6f7e` | Apache-2.0 | `@bubstack/moe-regressed` |
